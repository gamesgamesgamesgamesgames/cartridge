import { type Metadata } from 'next'

// Local imports
import { ContributePage } from '@/components/ContributePage/ContributePage'

export const metadata: Metadata = { title: 'Suggest Edit' }

export default ContributePage
