import { type Metadata } from 'next'

// Local imports
import { AccountsSettingsPage } from '@/components/SettingsPage/AccountsSettingsPage'

export const metadata: Metadata = { title: 'Connected Accounts' }

export default AccountsSettingsPage
